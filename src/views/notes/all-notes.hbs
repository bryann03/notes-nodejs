<div class="row">
    {{#each datosNotes}}
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title d-flex justify-content-between align-center">
                        {{title}}
                        <a href="/notes/edit/{{_id}}"><i class="fas fa-edit"></i></a>
                    </h4>
                    <p>{{description}}</p>
                    <button type="button" class="btn btn-danger btn-block btn-sm">Delete</button>
                </div>
            </div>
        </div>
    {{else}}
        <div class="card mx-auto">
            <div class="card-body">
                <p class="lead">There not notes yet..</p>
                <a href="/notes/add" class="btn btn-success btn-block">Create 0ne!</a>
            </div>
        </div>
    {{/each}}
</div>